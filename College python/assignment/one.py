box = {'Name': 'Nirdosh Kushwaha',
        'Class': 'BCA (B)',
        'Session': '22 - 25'}

for key, value in box.items():
    print(f"{key}: {value}")


# an f-string is a string that has expressions inside braces ({}) which are evaluated at runtime. When an f-string is prefixed with an f or F, Python understands that it's an f-string and allows expressions and variables to be embedded within the string.